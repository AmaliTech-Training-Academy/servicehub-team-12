<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.5</version>
        <relativePath /> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.servicehub</groupId>
    <artifactId>servicehub</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>servicehub</name>
    <description>Demo project for Spring Boot</description>
    <url />
    <licenses>
        <license />
    </licenses>
    <developers>
        <developer />
    </developers>
    <scm>
        <connection />
        <developerConnection />
        <tag />
        <url />
    </scm>
    <properties>
        <java.version>25</java.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
        <!-- Override the Byte Buddy version managed by Spring Boot parent.
             Byte Buddy 1.15.x is the first series to officially support
             Java 25 (class-file version 69). Without this override Mockito's
             inline mock maker throws:
             "Java 25 (69) is not supported … update Byte Buddy" -->
        <byte-buddy.version>1.15.11</byte-buddy.version>

        <!-- ===== Security vulnerability overrides =====
             These override Spring Boot 3.4.5's managed versions to pull in
             patched releases for the following CVEs:
             - GHSA-72hv-8253-57qq (HIGH 7.5):  jackson-core async parser DoS
               Affects: com.fasterxml.jackson.core:jackson-core:2.18.3
               Fix:     upgrade jackson-bom to 2.18.6+
             - CVE-2025-48924 (MEDIUM 5.3): commons-lang3 uncontrolled recursion
               Affects: org.apache.commons:commons-lang3:3.17.0
               Fix:     upgrade commons-lang3 to 3.18.0+
             - CVE-2025-41242 (MEDIUM 5.9): spring-webmvc path traversal
               Affects: org.springframework:spring-webmvc:6.2.6
               Fix:     upgrade spring-framework to 6.2.10+
             - CVE-2025-49146 (HIGH 8.2): pgjdbc allows fallback to insecure auth
               despite channelBinding=require configuration (MITM risk)
               Affects: org.postgresql:postgresql:42.7.4
               Fix:     upgrade postgresql to 42.7.7+
             - CVE-2025-41232 (CRITICAL 9.1) / CVE-2025-41248 (HIGH 7.5):
               Spring Security authorization bypass for method security annotations
               Affects: org.springframework.security:spring-security-core:6.4.5
               Fix:     upgrade spring-security to 6.4.10+
             - CVE-2025-46701 et al. (up to CRITICAL 9.6): multiple Tomcat CVEs
               including DoS, path traversal, constraint bypass, MITM
               Affects: org.apache.tomcat.embed:tomcat-embed-core:10.1.40
               Fix:     upgrade tomcat to 10.1.50+
             - CVE-2025-11226 (MEDIUM) / CVE-2026-1225 (LOW):
               Logback ACE via malicious config / class instantiation
               Affects: ch.qos.logback:logback-core:1.5.18
               Fix:     upgrade logback to 1.5.25+
             - CVE-2025-53864 (MEDIUM 5.8): Nimbus JOSE+JWT DoS via deeply nested JSON
               Affects: com.nimbusds:nimbus-jose-jwt:9.37.3
               Fix:     upgrade nimbus-jose-jwt to 9.37.4+
             - CVE-2026-24400 (HIGH 7.3, test-scope): AssertJ XXE via isXmlEqualTo
               Affects: org.assertj:assertj-core:3.26.3
               Fix:     upgrade assertj to 3.27.7+
             - CVE-2025-41249 (HIGH 7.5): Spring Framework annotation detection
               Affects: org.springframework:spring-core:6.2.10
               Fix:     upgrade spring-framework to 6.2.11+ -->
        <jackson-bom.version>2.18.6</jackson-bom.version>
        <commons-lang3.version>3.19.0</commons-lang3.version>
        <spring-framework.version>6.2.11</spring-framework.version>
        <postgresql.version>42.7.7</postgresql.version>
        <spring-security.version>6.4.10</spring-security.version>
        <tomcat.version>10.1.50</tomcat.version>
        <logback.version>1.5.25</logback.version>
        <nimbus-jose-jwt.version>9.37.4</nimbus-jose-jwt.version>
        <assertj.version>3.27.7</assertj.version>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
         <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>
        <dependency>
            <groupId>org.thymeleaf.extras</groupId>
            <artifactId>thymeleaf-extras-springsecurity6</artifactId>
        </dependency>

        <!-- Swagger UI / OpenAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.6</version>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.6</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Google OAuth2 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>



        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok-mapstruct-binding</artifactId>
            <version>${lombok-mapstruct-binding.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- JUnit 5 + Mockito + AssertJ + Spring Test (the only correct umbrella test starter) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Spring Security MockMvc support for controller-layer tests -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Surefire: enable Byte Buddy experimental mode so Mockito can
                 instrument interfaces and JDK classes under Java 25. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>
                        -Dnet.bytebuddy.experimental=true
                        --add-opens java.base/java.lang=ALL-UNNAMED
                        --add-opens java.base/java.util=ALL-UNNAMED
                    </argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>${lombok-mapstruct-binding.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <configLocation>google_checks.xml</configLocation>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>local-git-hooks</id>
            <activation>
                <file>
                    <exists>${basedir}/../ops-engine-room/git-hooks/pre-commit.py</exists>
                </file>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>install-git-pre-commit-hook</id>
                                <phase>initialize</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <mkdir dir="${project.basedir}/../.git/hooks" />
                                        <copy
                                            file="${project.basedir}/../ops-engine-room/git-hooks/pre-commit.py"
                                            tofile="${project.basedir}/../.git/hooks/pre-commit"
                                            overwrite="true" />
                                        <chmod file="${project.basedir}/../.git/hooks/pre-commit"
                                            perm="755" />
                                        <copy
                                            file="${project.basedir}/../ops-engine-room/git-hooks/commit-msg.py"
                                            tofile="${project.basedir}/../.git/hooks/commit-msg"
                                            overwrite="true" />
                                        <chmod file="${project.basedir}/../.git/hooks/commit-msg"
                                            perm="755" />
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>