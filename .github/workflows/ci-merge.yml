name: Merge CI

on:
    push:
        branches: [main]
        paths:
            - backend/**
            - data-engineering/**
            - .github/workflows/**

permissions:
    contents: read
    id-token: write 

jobs:
    ci:
        name: Backend merge checks + image build
        uses: ./.github/workflows/ci-reusable.yml
        with:
            run_checkstyle: true
            run_tests: true
            build_image: true
            push_image: false
            run_data_tests: true
            run_checkov: true
            run_trivy: false
            run_sbom: true
            deploy_dags: false
        secrets: inherit
