# Images

Place the admin dashboard screenshot here as:

`admin-dashboard-placeholder.png`

The root `README.md` already references that filename, so replacing it with the final image will make it show automatically.
